                        // openCamera là Tên module exports
const openStream = require('./openStream');


openStream();




//peer.on("connect", () => {
    // Copy offer qua answer, copy answer qua offer
//    setInterval(() => {
//        peer.send(Math.random());
//    }, 2000);
//});

// Gửi dữ liệu qua lại nhau, dữ liệu ở 2 trình duyệt sẽ khác như đang ở 2 máy khác
//peer.on('data', data => console.log("NHAN DU LIEU: " + data ));

